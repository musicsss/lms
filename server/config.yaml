server:
  port: 8080
  mode: debug

database:
  host: db
  port: 5432
  user: lms
  password: lms123
  dbname: lms
  sslmode: disable

jwt:
  secret: change-me-in-production
  expirehour: 72

storage:
  driver: local
  root: ./data

log:
  dir: ./logs
  maxsizemb: 20
  maxbackups: 20
